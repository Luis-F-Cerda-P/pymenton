<%= form_with(model: applicability_type) do |form| %>
  <% if applicability_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(applicability_type.errors.count, "error") %> prohibited this applicability_type from being saved:</h2>

      <ul>
        <% applicability_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
