<%= form_with(model: contact_mechanism_type) do |form| %>
  <% if contact_mechanism_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(contact_mechanism_type.errors.count, "error") %> prohibited this contact_mechanism_type from being saved:</h2>

      <ul>
        <% contact_mechanism_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
