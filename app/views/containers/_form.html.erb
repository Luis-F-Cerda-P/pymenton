<%= form_with(model: container) do |form| %>
  <% if container.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(container.errors.count, "error") %> prohibited this container from being saved:</h2>

      <ul>
        <% container.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.label :facility_id, style: "display: block" %>
    <%= form.text_field :facility_id %>
  </div>

  <div>
    <%= form.label :container_type_id, style: "display: block" %>
    <%= form.text_field :container_type_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
