<%= form_with(model: goods_order_detail) do |form| %>
  <% if goods_order_detail.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(goods_order_detail.errors.count, "error") %> prohibited this goods_order_detail from being saved:</h2>

      <ul>
        <% goods_order_detail.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :quantity, style: "display: block" %>
    <%= form.number_field :quantity %>
  </div>

  <div>
    <%= form.label :price, style: "display: block" %>
    <%= form.number_field :price %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
