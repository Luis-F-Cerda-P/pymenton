<%= form_with(model: goods_order_type) do |form| %>
  <% if goods_order_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(goods_order_type.errors.count, "error") %> prohibited this goods_order_type from being saved:</h2>

      <ul>
        <% goods_order_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
