<%= form_with(model: identification_type) do |form| %>
  <% if identification_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(identification_type.errors.count, "error") %> prohibited this identification_type from being saved:</h2>

      <ul>
        <% identification_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
