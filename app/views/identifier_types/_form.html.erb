<%= form_with(model: identifier_type) do |form| %>
  <% if identifier_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(identifier_type.errors.count, "error") %> prohibited this identifier_type from being saved:</h2>

      <ul>
        <% identifier_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
