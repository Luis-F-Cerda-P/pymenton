<%= form_with(model: juanca) do |form| %>
  <% if juanca.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(juanca.errors.count, "error") %> prohibited this juanca from being saved:</h2>

      <ul>
        <% juanca.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nombre, style: "display: block" %>
    <%= form.text_field :nombre %>
  </div>

  <div>
    <%= form.label :apellido, style: "display: block" %>
    <%= form.text_field :apellido %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
