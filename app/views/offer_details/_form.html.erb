<%= form_with(model: offer_detail) do |form| %>
  <% if offer_detail.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(offer_detail.errors.count, "error") %> prohibited this offer_detail from being saved:</h2>

      <ul>
        <% offer_detail.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :product_id, style: "display: block" %>
    <%= form.text_field :product_id %>
  </div>

  <div>
    <%= form.label :offer_id, style: "display: block" %>
    <%= form.text_field :offer_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
