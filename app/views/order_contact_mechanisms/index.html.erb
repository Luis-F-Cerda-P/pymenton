<p style="color: green"><%= notice %></p>

<h1>Order contact mechanisms</h1>

<div id="order_contact_mechanisms">
  <% @order_contact_mechanisms.each do |order_contact_mechanism| %>
    <%= render order_contact_mechanism %>
    <p>
      <%= link_to "Show this order contact mechanism", order_contact_mechanism %>
    </p>
  <% end %>
</div>

<%= link_to "New order contact mechanism", new_order_contact_mechanism_path %>
