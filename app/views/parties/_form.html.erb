<%= form_with(model: party) do |form| %>
  <% if party.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(party.errors.count, "error") %> prohibited this party from being saved:</h2>

      <ul>
        <% party.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.label :party_type_id, style: "display: block" %>
    <%= form.text_field :party_type_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
