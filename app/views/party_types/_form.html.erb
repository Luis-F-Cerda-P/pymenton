<%= form_with(model: party_type) do |form| %>
  <% if party_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(party_type.errors.count, "error") %> prohibited this party_type from being saved:</h2>

      <ul>
        <% party_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
