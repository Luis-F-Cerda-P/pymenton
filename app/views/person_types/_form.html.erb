<%= form_with(model: person_type) do |form| %>
  <% if person_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(person_type.errors.count, "error") %> prohibited this person_type from being saved:</h2>

      <ul>
        <% person_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
