<%= form_with(model: shipment_status) do |form| %>
  <% if shipment_status.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(shipment_status.errors.count, "error") %> prohibited this shipment_status from being saved:</h2>

      <ul>
        <% shipment_status.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :shipment_id, style: "display: block" %>
    <%= form.text_field :shipment_id %>
  </div>

  <div>
    <%= form.label :shipment_status_type_id, style: "display: block" %>
    <%= form.text_field :shipment_status_type_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
